<nav class="navbar navbar-expand-lg navbar-dark bg-primary container-fluid">
    <div class="container">
        {{#if user}} <a class="navbar-brand" href="/modifyUser">{{user.name}} </a>{{/if}}
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item active">
                    <a class="nav-link" href="/">Home <span class="sr-only">(current)</span></a>
                </li>
                {{#if user}}
                <li class="nav-item active">
                    <a class="nav-link" href="/location">new Location <span class="sr-only">(current)</span></a>
                </li>
                {{/if}}
                {{#if user.isAdmin}}
                <li class="nav-item active">
                    <a class="nav-link" href="/admin">admin panal <span class="sr-only">(current)</span></a>
                </li>
                {{/if}}
            </ul>
            {{#if user}}
            <a href="/logout" class=" my-2 my-lg-0  btn btn-primary">
                Logout
            </a>
            {{else}}
            <a href="/login" class=" my-2 my-lg-0  btn btn-primary">
                Login
            </a>
            {{/if}}
        </div>
    </div>
</nav>